import CrRowsCard from '../components/tracker/CrRowsCard'

/** Overall Status numbers live in Configure → Overall Status Tiles and render in
    the Briefing; the Tracker is just the working row table. */
export default function TrackerView() {
  return <CrRowsCard />
}
